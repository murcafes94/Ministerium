# Android / Gradle
.gradle/
.idea/
local.properties
**/build/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Generated installables and signing material (publish as Release assets, never source)
*.apk
*.aab
*.apks
*.jks
*.keystore
*.p12
*.pem
*.key

# Secrets / local environment
.env
.env.*
!.env.example
secrets.properties
keystore.properties

# Liturgia Papal preprocessing.
# The source PDFs stay only in the local cache and the generated compact package
# is produced locally/CI before compiling; neither is source code in the repo.
tools/cache/missal-pdf/
app/src/main/assets/missal/

# OS / editor / temporary files
.DS_Store
Thumbs.db
*~
*.tmp
*.temp
*.swp
*.swo
.*.epub.*
